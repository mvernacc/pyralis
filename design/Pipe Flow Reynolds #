% Matlab code used to produce pipe flow reynolds numbers.

function [ Re ] = Re( mu, mdot, d )
%Reynolds number calculator for pipe flow.
%   Form: Re(dynamic viscosity, mass flow rate, pipe diameter)
Re = (4 * mdot)./(pi * mu * d);

end
